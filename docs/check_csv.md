# Validate CSV Files

The Comma Separated Values (CSV) file format is the most common import formats for spreadsheets and databases. But loading csv files can result in a security nightmare.

For CSV, the main security concerns are: resource exhaustion, malformed/oversized records, dangerous spreadsheet formulas, unexpected encodings/control characters, and unsafe remote URLs.
